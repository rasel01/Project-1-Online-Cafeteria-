@model UiuCanteen.Domain.Entities.ShippingDetails
           
@{
    ViewBag.Title = "UIU Canteen: Checkout";
}
<h2>Check out now</h2>
<p>Please enter your details, and get what you want</p>
@using (Html.BeginForm())
{
    @Html.ValidationSummary()
    <h3>Order From</h3>
    <div class="form-group">
        <label>Name:</label>
        @Html.TextBoxFor(x => x.Name, new {@class = "form-control"})
    </div>
    <div class="form-group">
        <label>Mobile Number Or Id Number:</label>
        @Html.TextBoxFor(x => x.Number, new { @class = "form-control" })
    </div>
    <div class="form-group">
        <label>Secret Word:</label>
        @Html.TextBoxFor(x => x.SecretWord, new { @class = "form-control" })
    </div>
    @*foreach (var property in ViewData.ModelMetadata.Properties)
    {
        if (property.PropertyName != "Name" && property.PropertyName !=
            "SecretWord")
        {
            <div class="form-group">
                <label>
                    @(property.DisplayName ?? property.PropertyName)
                </label>
                @Html.TextBox(property.PropertyName, null, new
               {
                   @class = "form-control"
               })
            </div>
        }
    }*@

    <div class="text-center">
        <input class="btn btn-primary" type="submit" value="Complete order" />
    </div>

}